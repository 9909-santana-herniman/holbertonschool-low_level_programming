#include "main.h"

/**
 * rev_string - A function that reverse a string
 * @s: The string to be printed
 * Return: Void
 */

void rev_string(char *s)
{
	int a;
	int b;
	char *start = s;

	while (*s != '\0')
	{
		s = s + 1;
	}
	s = s - 1;
	while (s >= start)
	{
		
	}
	_putchar('\n');
}

