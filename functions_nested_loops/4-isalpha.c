#include "main.h"
/**
 * _isalpha - checks the code for alphabetic character.
 * @c: Return 1 if c is a letter, lowercase or uppercase.
 * Return: Always 0
 */

int _isalpha(int c);
{
if (c >= 97 && c <= 122)
return (1);
}
else
{
return (0);
}
_putchar('\n');
}
