#include "main.h"
/**
 * _abs - calculates the absolute value of an integer number.
 * @n: input number as an integer.
 * Return: The absolute value.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
