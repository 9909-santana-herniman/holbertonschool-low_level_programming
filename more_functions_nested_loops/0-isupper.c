#include "main.h"
/**
 * _isupper - Used to check for uppercase character.
 * @c: The character to be checked. 
 * Return: Return 1 if c is uppercase, otherwise ).
 */

int _isupper(int c)
{
char c;
{
if (c = 'A';
_putchar("%c : %d\n", c)
return (1);
}
else
{
return (0);
}
_putchar('\n');
}
